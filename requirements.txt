# =====================================================================
# 数值栈【钉上限】：整套已在 Python 3.12 + numpy 1.26 / pandas 2.2 / sklearn 1.4/1.5
# 下完整测试通过。numpy 2.x 与 scikit-learn>=1.6 会在稀疏/全 NaN 特征列的分箱路径抛
# "window shape cannot be larger than input array shape"（本地旧版无法复现、CI 装最新版
# 才触发），故统一钉住大版本，保证 GitHub Actions 与本地验证环境严格一致、可重复构建。
# 升级这些版本前，请先在干净虚拟环境跑通 `pytest -q` 全绿再放开上限。
# =====================================================================
# ---- 核心数据处理（钉版本，保证 CI=本地已验证环境）----
numpy>=1.26,<2.0
pandas>=2.1,<2.3
scipy>=1.11,<1.14
PyYAML>=6.0
requests>=2.31
feedparser>=6.0.10
beautifulsoup4>=4.12
lxml>=5.0
tqdm>=4.66
xlrd>=2.0.1            # 读取 GPRD 地缘风险指数的 .xls 备份地址（主地址失效时启用）
openpyxl>=3.1

# ---- 建模（钉 sklearn<1.6、statsmodels 0.14.x，兼容 numpy 1.26）----
scikit-learn>=1.3,<1.6
statsmodels>=0.14,<0.15
# Prophet 与 XGBoost 为可选增强（体积较大，默认不安装也可运行全流程）：
# prophet>=1.1.5
# xgboost>=2.0

# ---- 行情数据源（无需 API Key 的真实行情兜底源） ----
yfinance>=0.2.40

# ---- 可视化与交互报告 ----
plotly>=5.18,<6
jinja2>=3.1
streamlit>=1.32,<2

# ---- 测试 ----
pytest>=7.4
joblib>=1.3
